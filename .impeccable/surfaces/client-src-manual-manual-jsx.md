---
version: 1
slug: "client-src-manual-manual-jsx"
primary_target: "client/src/manual/Manual.jsx"
related_targets: ["client/src/manual/manual.css", "client/src/manual/FaceplateFigure.jsx"]
---

# Surface: Operator's Primer (/manual)

Scope: the how-to-play guide printed as the opening pages of a station logbook — how the band works, why copy garbles, the controls, a worked contact line by line, QSL cards and the shack, the standing beacons, and a lingo glossary. Visitor mode: Read (comprehension leads; the paper carries the charm).

Audience & job: newcomers who need the full choreography of a first QSO before keying; real hams checking the lingo is used right. Reached from the rig's instruction plate (READ THE OPERATOR'S PRIMER →, opens a new tab so the rig stays on the air) and the shack header's PRIMER back-link.

Structure: "The Logbook Primer", seed guide5eed, code-led: one typewritten band-post sheet (double-rule masthead + READ BEFORE KEYING rubber stamp), §1–§6 across two 2-column spreads flanking FIG. 1 — a one-ink patent figure of the TD-40 faceplate with eight numbered margin bubbles on bowed leaders and a parts list — plus a ruled worked-contact log page with madder margin annotations behind a red margin rule, a rotated ruled glossary index card, a beacon table, and a double-rule footer (PRINTED BY THE DIAL BAND POST · NOTHING ELSE IS).

Register: sentence-case typewritten body (14px/1.7, 72ch) under The Paper Read Register — the finish review accepted the adaptation citing Read-mode comprehension; silk stays all-caps for mastheads, §-titles, glossary terms, table headers, stamps. Print fine-type steps 13.5/13/12.5/12 each own one role (canonized as built; the reviewer's suggestion to thin the band was declined). Em emphasis is the typewriter underline, never oblique.

Protected (finish-review keep line, disposition ship): the madder margin annotations on the worked log, the one-ink patent restraint of FIG. 1, and the sentence-case typewritten register — the page's comprehension engine.

States: static printed matter — no loading, empty, or error states; fully readable unpowered and muted. At ≤820px the spreads collapse, the index card squares up, and the stamp is dispensed with.

Unresolved: none blocking; the only open charter question from review — thinning the 12–13.5px fine band — was resolved by canonizing the built steps.
